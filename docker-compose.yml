version: '3'
services:
    app:
        build: ./
        working_dir: /app
        volumes:
          - ./:/app
        links:
          - redis:redis
        ports:
          - "81:8081"
        command: ./run.sh
    redis:
        image: redis
    redis_commander:
        image: tenstartups/redis-commander
        command: --redis-host redis
        links:
          - redis:redis
        ports:
          - "8081:8081"